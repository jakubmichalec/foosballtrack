<% if user_signed_in? %>
  <%= link_to "New Player", new_player_path %>
  <%= link_to "New Match", new_match_path %>
<% end %>

<% @players.each do |player| %>
  <h2><%= link_to player.full_name, player %></h2>
<% end %>
